# OP-Geth for Base Sepolia
# Minimal resources for testnet

resources:
  limits:
    cpu: 1000m
    memory: 4Gi
  requests:
    cpu: 500m
    memory: 2Gi

persistence:
  size: 100Gi

nodeSelector:
  workload: general

tolerations: []

config:
  network: "base-sepolia"
  sequencerUrl: "https://sepolia-sequencer.base.org"
  rollup:
    syncMode: "execution-layer"
  cache: 1024
  gcMode: "full"

