# OP-Geth for Optimism Sepolia
# Minimal resources for testnet

resources:
  limits:
    cpu: 1000m
    memory: 4Gi
  requests:
    cpu: 500m
    memory: 2Gi

persistence:
  size: 100Gi

nodeSelector:
  workload: general

tolerations: []

config:
  network: "op-sepolia"
  sequencerUrl: "https://sepolia-sequencer.optimism.io"
  rollup:
    syncMode: "execution-layer"
  cache: 1024
  gcMode: "full"

