# BSC Geth for Chapel Testnet
# Minimal resources for testnet

resources:
  limits:
    cpu: 1500m
    memory: 6Gi
  requests:
    cpu: 500m
    memory: 2Gi

persistence:
  size: 100Gi

nodeSelector:
  workload: general

tolerations: []

config:
  network: "chapel"
  syncMode: "snap"
  cache: 1024
  gcMode: "full"

