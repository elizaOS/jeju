# Jeju IPFS Storage Service - Environment Variables

# ============ Service Configuration ============
# Port for the Pinning API server (HTTP endpoints)
PORT=3100
IPFS_PORT=3100

# ============ IPFS Node Configuration ============
# Kubo IPFS daemon connection (MOVED TO 41xx RANGE)
IPFS_NODE_HOST=localhost
IPFS_NODE_PORT=4100

# ============ Database Configuration ============
# PostgreSQL connection for pin tracking
DATABASE_URL=postgresql://localhost:5432/jeju_ipfs

# ============ Payment Configuration ============
# Address to receive x402 payments
PAYMENT_RECEIVER_ADDRESS=0x0000000000000000000000000000000000000000

# Base RPC for payment verification
BASE_RPC_URL=https://mainnet.base.org

# ============ Gateway Configuration ============
# URL for gateway links in responses
GATEWAY_URL=http://localhost:3100

# ============ Development Mode ============
# Bypass x402 payments in development
NODE_ENV=development

