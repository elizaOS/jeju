{
  "name": "@jeju/rpc-gateway",
  "version": "1.0.0",
  "description": "RPC Gateway with staking-based rate limits and X402 payments",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test",
    "test:unit": "bun test tests/unit",
    "test:integration": "bun test tests/integration",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "hono": "^4.6.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10",
    "typescript": "^5.6.0"
  }
}
